// This file is used to configure and set up the Jest testing environment before each test runs.

// Common use cases include importing custom matchers (like @testing-library/jest-dom), setting global variables,
// For example, after importing @testing-library/jest-dom, we can use matchers like:
//   expect(element).toBeInTheDocument();

import '@testing-library/jest-dom';
